#include<stdio.h>
void main()
{
    int a,f;
    printf("\n Enter a number:");
    scanf("%d",&a);
    if(a>0)
    {
        printf("\n output is:%d",a);
    }
    else(0>a);
    {
        f=(0-a);
        printf("The output is:%d",f);
    }
}